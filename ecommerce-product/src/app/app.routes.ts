import { Routes } from '@angular/router';

import { PageMainComponent } from './domains/main/page-main/page-main.component';

export const routes: Routes = [
    {
        path: '',
        component: PageMainComponent
    }
];
